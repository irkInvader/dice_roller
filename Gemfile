source 'https://rubygems.org'
ruby '2.2.2'

gem 'rails', '4.2.1'
gem 'sass-rails', '~> 5.0'  # Use SCSS for stylesheets
gem 'uglifier', '>= 1.3.0'  # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.1.0'  # Use CoffeeScript for .coffee assets and views
gem 'jquery-rails'  # Use jquery as the JavaScript library
gem 'jbuilder', '~> 2.0'  # Build JSON APIs; https://github.com/rails/jbuilder
gem 'bcrypt', '~> 3.1.7'  # Use ActiveModel has_secure_password
gem 'unicorn'  # Use Unicorn as the app server

gem 'bootstrap-sass'
gem 'figaro', '>= 1.0.0.rc1'
gem 'high_voltage'
gem 'omniauth'
gem 'omniauth-google-oauth2'
gem 'pg'
gem 'pundit'
gem 'slim-rails'
gem 'unicorn'
gem 'unicorn-rails'


group :development do
  gem 'better_errors'
  gem 'haml2slim'
  gem 'html2haml'
  gem 'quiet_assets'
  gem 'rails_apps_pages'
  gem 'rails_layout'
end

group :development, :test do
  gem 'byebug'  # Call 'byebug' in the code to stop exec and get debug console
  gem 'web-console', '~> 2.0'  # IRB console in exceptions, or <%= console %>
  gem 'spring'  # Speed up dev; Read more: https://github.com/rails/spring

  gem 'pry-rails'
  gem 'pry-rescue'
  gem 'rubocop'
end
